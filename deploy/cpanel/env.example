# ==== Meelano — متغیرهای محیطی روی cPanel (در همان Setup Node.js App پر کنید) ====
ADMIN_USER=admin
ADMIN_PASS=CHANGE_ME_STRONG_16c
# --- اتصال MySQL هاست (اختیاری در ابتدا) ---
# پس از «اعمال» در بخش تنظیمات پنل، کانفیگ در data/db-config.json ذخیره می‌شود و
# لازم نیست این‌جا چیزی بگذارید. برای force از ابتدا:
# (پل MySQL از تنظیمات پنل مدیریت می‌شود؛ این‌جا فقط برای بوت سریع‌تر)
# --- سرویس‌ها ---
# OPENAI_API_KEY=            # تولید کپشن AI واقعی
# OPENAI_BASE_URL=
# ZARINPAL_MERCHANT_ID=      # درگاه واقعی؛ بدون آن حالت دمو است
# SMS_API_KEY=
# CSP_FRAME_ANCESTORS=
